<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jhta.mybatis.mapper.MembersMapper">	
	<select id="select" parameterType="string" resultType="membersVo">
		select * from members where addr = #{addr}
	</select>
</mapper>
